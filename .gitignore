# Local environment (secrets) — never commit.
.env

# Flarum data (when bind-mounted locally).
flarum-data/
flarum_data/

# Backup/restore drop-zone — never commit backups (they contain forum data).
restore/
!restore/.gitkeep

# Editor / OS cruft.
*.swp
.DS_Store
